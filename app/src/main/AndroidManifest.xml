<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fakechat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FakeChat">
        <activity android:name=".Pro_movie"></activity>
        <activity android:name=".Pro_shop" />
        <activity android:name=".Pro_food" />
        <activity android:name=".Map_movie" />
        <activity android:name=".Map_shop" />
        <activity android:name=".Map_food" />
        <activity android:name=".Pro" />
        <activity android:name=".Map" />
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>